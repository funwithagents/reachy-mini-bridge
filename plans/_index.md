# Implementation plans

Implementation plans for Reachy Mini Bridge — each plan turns a settled part of a spec (see [specs/_index.md](../specs/_index.md)) into concrete, buildable steps. Plans are ordered by their date-time filename prefix (`YYYYMMDDHHmm_`).

## Plans

<!-- One row per plan, chronological by filename prefix. Keep the Status column in sync with each plan's `**Status:**` line. -->

| Plan | Description | Status |
|---|---|---|
| [202609041726_client-seam-and-fake-reachy-mini.md](202609041726_client-seam-and-fake-reachy-mini.md) | Build the `client.py` seam: `RobotClient` union alias, `build_robot(...)` factory (lazy upstream import), and the `FakeReachyMini` stand-in for the v1 consumed slice; adds `numpy`/`reachy_mini` base deps + `sim` extra | Done |
| [202609071702_e2e-targets-and-capability-gating.md](202609071702_e2e-targets-and-capability-gating.md) | Turn the sim-only e2e fixture into a target-selectable (`sim` headless/headfull, `real`), capability-probing harness with a `requires_caps(...)` skip gate | Done |
| [202609081557_api-and-audio-layer.md](202609081557_api-and-audio-layer.md) | Build the coupled api + audio layer: async-native `ReachyMiniApi` (motors, expression, gaze, audio verbs) over the media session (`SpeechSynthesizer`, say sink, mic tap, conversion helpers, tts adapter); adds `samplerate` base dep + `tts` extra + `errors.py`, fast `tests/` + a capability-gated `tests-e2e/` tier (with a `live_api` fixture) that confirms the real audio format | Done |
| [202609091100_rename-robotclient-to-anyreachymini.md](202609091100_rename-robotclient-to-anyreachymini.md) | Rename the seam's `RobotClient` union alias → `AnyReachyMini` and import `reachy_mini` normally (drop `TYPE_CHECKING`/lazy import); fixes the `.client`-on-a-`Client` name clash and IDE go-to-definition landing on the fake | Done |
| [202609091430_split-fake-and-rename-client-to-robot.md](202609091430_split-fake-and-rename-client-to-robot.md) | Split `FakeReachyMini` (+ helpers) into a new `fake_reachy_mini.py`; rename `client.py`/`client.md` → `robot.py`/`robot.md` (the seam keeps just `AnyReachyMini` + `build_robot`); split the seam tests into `test_robot.py` + `test_fake_reachy_mini.py` | Done |
| [202609091700_camera-frame-perception-verb.md](202609091700_camera-frame-perception-verb.md) | Add a `get_camera_frame` perception verb (raw BGR `ndarray`\|`None`, mirroring upstream `media.get_frame`) to the v1 api; move the live camera test onto the API and delete `tests-e2e/test_robot.py` (its motor-state check is already covered at the API layer) | Done |
| [202609101200_ship-testing-support-harness.md](202609101200_ship-testing-support-harness.md) | Promote the live-tier harness from `tests-e2e/` into a shipped `reachy_mini_bridge.testing` package (opt-in pytest plugin, `test` extra, `live_api`/`requires_caps`/`require_env`) with a consumer guide, so downstream projects test against `fake`/`sim`/`real` without re-deriving the daemon lifecycle | Done |
| [202609141536_config-and-daemon-lifecycle.md](202609141536_config-and-daemon-lifecycle.md) | Build `ReachyMiniConfig` (`from_dict`/`from_json`/`from_json_file`; `robot` kwargs verbatim, `daemon`, `tts`, `audio` blocks) and the bridge-owned `daemon.py` (own-it-or-borrow-it sim daemon); construct `ReachyMiniApi` from the config with robot construction moved into `__aenter__`, the `tts` block building the default synthesizer; rebuild `testing/_daemon.py` on `daemon.py` | Done |
| [202609141800_degradable-default-voice.md](202609141800_degradable-default-voice.md) | A `tts` block whose adapter cannot be built (e.g. `api_key_env` unset) no longer fails `ReachyMiniApi` construction: the api comes up with no voice, `synthesizer_error` exposes the cause, `say` raises `BridgeError` chained to it (the missing `tts` extra stays a hard `ConfigError`) | Done |
| [202609141900_say-completion-and-wobbling.md](202609141900_say-completion-and-wobbling.md) | `say` completes when the utterance has finished playing (wall-clock playback-end tracker + tail margin) and flushes the speaker on cancel or synthesizer failure (fixes `specs/_tts-bug.md`); upstream's audio-reactive head wobbling, on by default through a top-level `wobbling` config flag applied on entry (off again at exit) plus a `set_wobbling` verb and `wobbling` property; fake grows the two toggles; live tests for completion time and observable sway | Done |
| [202609161507_real-robot-e2e-daemon-and-gravity-capability.md](202609161507_real-robot-e2e-daemon-and-gravity-capability.md) | The e2e harness spawns (and tears down) a local `reachy-mini-daemon` for the `real` target when none is ready (USB robot, loopback only; Placo engine when installed); gravity compensation becomes a probed `gravity_compensation` capability so the live tier skips it instead of losing the connection, and `set_motors_state` refuses it up front (`GravityCompensationUnsupportedError`) on a daemon without the Placo engine; the audio probe stops restarting the SDK's audio pipeline, which on macOS moved the pytest tier's say/mic audio onto the Mac's own speaker and mic; AGENTS.md gains a "Running the live e2e tests" section | Done |
| [202609161510_package-front-door-and-fake-fidelity.md](202609161510_package-front-door-and-fake-fidelity.md) | Replace the `hello()` scaffold with a package front door (`from reachy_mini_bridge import ReachyMiniApi`, plus the synthesizer and error types); trim the uncalled `goto_target` from the fake; align `apply_audio_config`'s default with upstream, guarded by a new fake↔upstream signature-parity test | Done |
| [202609161520_media-session-lifecycle-hardening.md](202609161520_media-session-lifecycle-hardening.md) | `AsyncExitStack` lifecycle for `MediaSession` and `ReachyMiniApi` (failed opens unwind, teardown survives a failing step); `say`/`audio_input` require an open session (`BridgeError`); the mic tap stops busy-polling on `None` and ends when the session closes | Done |
| [202609161522_real-daemon-from-the-api.md](202609161522_real-daemon-from-the-api.md) | `ReachyMiniApi` spawns (and tears down) the hardware daemon of a USB-attached robot: `daemon.spawn` opens to `backend: "real"` (loopback only; `preload_datasets` / `startup_timeout` apply, the sim knobs `headless` / `scene` play no part), and the api passes its backend to `managed_daemon` | Done |
| [202609161900_cancellable-verbs.md](202609161900_cancellable-verbs.md) | The "Cancellation" contract of `api.md`: a cancelled `play_emotion` stops motion *and* sound (a `MediaSession.stop_sound()` that leaves the shared pipeline alone — the local `playbin` set to `NULL` pending the upstream fix in `docs/upstream-play-move-cancellation.md`), the fake keeps move timing, the emotions-library load is shielded, and bring-up (`__aenter__` of the api and the media session) unwinds on cancel instead of leaking a daemon or connection; fake-backed cancellation tests plus a live `dance2` cancel test | Done |
| [202609162000_motion-loop-presence-and-breathing.md](202609162000_motion-loop-presence-and-breathing.md) | Build `motion.py`'s `MotionSession` — the one 60 Hz `set_target` writer — with `BreathingMove` / `HoldMove` idle moves and blended transitions; re-base `play_emotion` on it (sound in the loop, tracking dip + wobbling pause restored on every exit path); add the `presence` / `breathing` switches (`motion` config block + verbs), motor-state pause/resume with re-anchoring, exit blend to neutral; fake gains `set_target` + pose readers and drops `async_play_move`; fast + live tests and an on-robot checklist | In progress |
| [202609170015_motion-config-consolidation-and-default-tracking.md](202609170015_motion-config-consolidation-and-default-tracking.md) | Move the top-level `wobbling` config flag into `MotionSettings` alongside `presence` / `breathing`; add a `tracking` switch (default on) realized at session entry when motors already read `enabled`, or on the next `set_motors_state("enabled")` otherwise, and stopped on exit like wobbling | Done |
| [202609171234_organic-breathing-rests-and-independent-antennas.md](202609171234_organic-breathing-rests-and-independent-antennas.md) | Rebuild `BreathingMove` as a seeded, lazily generated plan of rest-to-rest segments: raised-cosine breaths separated by random 1–5 s rests on the head's z axis, and two independent antenna tracks roaming 10°–25° outward from vertical (never converging); the breathing fade-out generalises to the plan; fast seeded `evaluate`-driven tests plus adjusted api / live tests | Done |
| [202609171258_attention-hand-the-head-back-when-alone.md](202609171258_attention-hand-the-head-back-when-alone.md) | An attention loop in `ReachyMiniApi`: while tracking is active, poll the daemon's face target and, after a grace period without a face, hand the head back to the idle move (re-arm tracking through weight 0 then a small watching weight, after a `MotionSession.reanchor()`), re-engaging the requested weight when a face reappears; `attention` property; fake `face_detected` + `get_tracked_face`; draft upstream report | Done |

## Status legend

- **Todo** — written, not yet started
- **In progress** — actively being implemented
- **Done** — implemented, verified (lint/type-check/tests pass), and merged
